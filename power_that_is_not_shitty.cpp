#include <bits/stdc++.h>
using namespace std;
long long power(long long  x, long long y)
{
      
   
    return (long long)pow(x, y);
}
  

int main()
{
    long long x; long long y;
    cin >> x >> y;
  
    cout << (power(x, y));
}